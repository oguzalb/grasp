class Person
    __init__(self, name) ->
        self.name = name
    hello(self) ->
        print(self.name)
    __getitem__(self, item) ->
        return item
person = Person("oguz")
person.hello()
print(person[0] + 1)
for i in range(10)
    print(i)
i = 4
i.nonexistent_field
